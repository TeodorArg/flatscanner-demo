You are adding the orchestration contract for implementation work in a repository that uses isolated workers.

Task:
- define or implement the minimal scripts and docs needed to support isolated implementation work

Required capabilities:
- choose the current implementation agent manually
- create an isolated branch/worktree from main
- launch the selected implementation agent against that worktree
- publish a PR for the resulting branch

Important constraints:
- keep the design minimal
- do not invent automatic failover logic
- do not add product-specific behavior
- document the contract even if parts remain manual in v1
- do not assume the original shell or OS
- define the behavior contract first, then implement it natively for the target environment

Expected outputs:
- workflow docs for the orchestration loop
- script contract or actual scripts for:
  - set current implementation agent
  - create worktree
  - start implementation worker
  - publish branch / PR

The resulting implementation may use:

- bash
- zsh
- python
- make / just / task
- another native automation layer

as long as it preserves the same workflow behavior.

Validation:
- explain the exact task lifecycle from main to PR
- explain how multiple agents are kept isolated
- explain how the script contract was adapted to the target OS/environment
